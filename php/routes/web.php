<?php

/*
|--------------------------------------------------------------------------
| Web Routes
|--------------------------------------------------------------------------
|
| Here is where you can register web routes for your application. These
| routes are loaded by the RouteServiceProvider within a group which
| contains the "web" middleware group. Now create something great!
|
*/

//Route::get('/', function () {
//    $articles = \App\Article::orderBy('created_at','desc')->with('catalog')->with('tags')->paginate(5);
//    return view('welcome',['articles'=>$articles]);
//});

//Route::get('/allids', 'ArticleController@getAllIds');


//Auth::routes();

//Route::get('/index', 'BlogController@index');
//Route::get('/getArticles', 'ArticleController@getArticles');
//
//Route::get('/article/{id}', 'ArticleController@showById');
//Route::group(['middleware' => 'auth'], function () {
//    Route::resource('articles', 'ArticleController');
//});